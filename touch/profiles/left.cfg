// touch/profiles/left.cfg — Left-handed mirror of standard.
// Centres use the same numbers as standard: Touch_MirrorX applies 1-x to every
// widget and flips the look and move zones, so handedness is the only override.

exec touch/profiles/standard.cfg

touch_preset "left"
touch_handedness 1
