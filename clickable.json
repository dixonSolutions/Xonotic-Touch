{
    "clickable_minimum_required": "7.0.0",
    "framework": "ubuntu-sdk-20.04",
    "builder": "cmake",
    "restrict_arch": "arm64",
    "prebuild": "test -d engine/data || (echo 'engine/data/ is missing; copy game data with: cp -a engine-src/data engine/data' >&2 && exit 1)",
    "install_data": {
        "engine/": "engine"
    },
    "ignore": [
        "engine-src"
    ]
}
