# Build artifacts
build/
.clickable/

# Xonotic source checkout (build locally with ./all update)
engine-src/

# Game data is copied from engine-src during build; keep binary tracked separately if needed
# Ignore all data files but explicitly track our touch config
engine/data/*
!engine/data/xonotic.cfg
